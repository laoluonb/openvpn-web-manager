[Unit]
Wants=openvpn-manager-firewall.service
After=openvpn-manager-firewall.service

[Service]
# Runtime directory ownership is managed centrally by tmpfiles.d.  Do not let
# the OpenVPN service recreate the shared directory with its own service user.
RuntimeDirectory=
