# 安全策略

## 支持范围

安全修复会应用到 `main` 分支的最新版本。

## 报告安全问题

请优先使用 GitHub 私有漏洞报告功能，不要直接提交公开 Issue。报告应包含受影响版本、复现步骤、
影响范围和建议修复方式。

## 部署注意事项

- Web 进程以普通用户运行，并通过受用户组限制的 Unix 套接字连接 root 控制代理。
- 安装器默认生成自签名 TLS 证书；生产环境应替换为受信任证书。
- 使用 `--web-allow`、云安全组或上游防火墙限制管理端口来源。
- 离线备份 `/etc/openvpn/server/easy-rsa/pki`，其中包含 CA 私钥。
- 不要把生成的 `.ovpn`、私钥、凭据或 PKI 文件提交到 Git。
